version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd chat-app
        - npm install
    build:
      commands:
        - docker-compose build
    postBuild:
      commands:
        - # Adicione comandos adicionais, se necessário
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
